class PiezaRepository {
    crearPieza(pieza) { throw new Error("Método no implementado"); }
    obtenerPiezas() { throw new Error("Método no implementado"); }
    obtenerPiezaPorId(id) { throw new Error("Método no implementado"); }
    actualizarPieza(id, pieza) { throw new Error("Método no implementado"); }
    eliminarPieza(id) { throw new Error("Método no implementado"); }
}

module.exports = PiezaRepository; // Exporta la clase para que pueda ser utilizada en otros módulos