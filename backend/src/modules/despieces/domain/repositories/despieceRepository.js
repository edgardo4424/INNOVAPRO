class DespieceRepository {
    crearDespiece(despiece) { throw new Error("Método no implementado"); }
    obtenerDespieces() { throw new Error("Método no implementado"); }
    obtenerDespiecePorId(id) { throw new Error("Método no implementado"); }
    actualizarDespiece(id, despiece) { throw new Error("Método no implementado"); }
    eliminarDespiece(id) { throw new Error("Método no implementado"); }
}

module.exports = DespieceRepository; // Exporta la clase para que pueda ser utilizada en otros módulos