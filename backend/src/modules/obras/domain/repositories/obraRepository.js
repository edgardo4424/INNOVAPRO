class ObraRepository {
    crearObra(obra) { throw new Error("Método no implementado"); }
    obtenerObras() { throw new Error("Método no implementado"); }
    obtenerObraPorId(id) { throw new Error("Método no implementado"); }
    actualizarObra(id, obra) { throw new Error("Método no implementado"); }
    eliminarObra(id) { throw new Error("Método no implementado"); }
}

module.exports = ObraRepository; // Exporta la clase para que pueda ser utilizada en otros módulos