class AtributosValorRepository {
    crearAtributosValor(atributos_valor) { throw new Error("Método no implementado"); }
    obtenerAtributosValor() { throw new Error("Método no implementado"); }
}

module.exports = AtributosValorRepository; // Exporta la clase para que pueda ser utilizada en otros módulos