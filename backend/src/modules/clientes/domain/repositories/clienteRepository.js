class ClienteRepository {
    crearCliente(cliente) { throw new Error("Método no implementado"); }
    obtenerClientes() { throw new Error("Método no implementado"); }
}

module.exports = ClienteRepository; // Exporta la clase para que pueda ser utilizada en otros módulos